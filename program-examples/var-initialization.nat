@main
{
    int x = 0 ;    
    ()
    f += a + b * c / d
    f *= a
    d /= c
    f -= a
    a++;
    b--;
    --c;
    ++g

    float g;
    set h;
    double a;
    bool m;
    tuple t;
    byte aaaa;

    for (int i=0; i < 100; i++){
        if (a > b ){

        }  else if ( a < b ){

        } else {
            
        }
    }

    while ( !(a && (b and c || a) or d ) ){
        a += 20;

    }

}

